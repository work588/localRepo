{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run C++",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++17",           // Use C++17 standard
                "-Wall",                // Show all warnings
                "${file}",              // Current file
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}", // Output exe in same folder
                "&&",
                "${fileDirname}/${fileBasenameNoExtension}"  // Run the exe
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compile and run C++ in terminal"
        }
    ]
}
